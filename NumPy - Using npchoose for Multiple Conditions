import numpy as np


indices = np.array([0, 1, 2, 0])
choices = [indices*2, indices+10, indices**2]

# Use np.choose with the corrected index array
result = np.choose(indices, choices)

print('Result using np.choose:', result)
